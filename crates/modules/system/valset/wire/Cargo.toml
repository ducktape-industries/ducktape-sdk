# The valset module's wire surface: generation sets, queries and replies, no
# module — the kernel host, the CLI and a sibling module link THIS and not the
# module behind it. No `native` feature, no kernel host, no disk.
[package]
name = "valset-wire"
edition.workspace = true
version.workspace = true

[dependencies]
sdk = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
futures = { workspace = true }
sdk-testkit = { workspace = true }
